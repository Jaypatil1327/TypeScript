const count = 100;
const count2 = 100;
const str = "Hello world";
function add(a, b) {
    return a + b;
}
let random;
random = Math.ceil(Math.random() * 100) > 5 ? 12 : "its greater than 5";
console.log(random);
export { add };
