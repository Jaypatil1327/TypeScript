const count = 100;
const count2 = 100;
const str = "Hello world";
function add(a, b) {
  return a + b;
}
let random;
random = Math.ceil(Math.random() * 10) > 5 ? 1 : "its greater than 5";
console.log(random);
export { add };
